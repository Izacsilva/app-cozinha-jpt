{
  "name": "app-cozinha-jpt",
  "version": "1.0.0",
  "description": "",
  "main": "Code.js",
  "type": "commonjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "node build-styles.js && node build-html.js",
    "push": "npm run build && clasp push",
    "build:gs": "esbuild apps-script/src/main.js --bundle --outfile=apps-script/dist/code.js --format=iife"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fs-extra": "^11.3.0",
    "glob": "^11.0.3",
    "sass": "^1.89.2"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
